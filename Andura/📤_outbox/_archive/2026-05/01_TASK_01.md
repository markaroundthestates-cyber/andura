# Phase 6 — Task 01 LANDED `c64e692`

**Date:** 2026-05-18
**Backup tag:** `pre-phase6-task-01-2026-05-18`
**Tests:** 4303 → 4318 PASS (+15) / TS 0 errors

## Commit
- `c64e692` feat(engine/schedule): scheduleAdapter.getDailyWorkout consumer runPipeline 8 adapters

## Changes
- M `src/engine/schedule/scheduleAdapter.js` — NEW `getDailyWorkout(userState, now)` async export consumes `runPipeline` 8 adapters + delegates to `buildSession(sessionType, ctx)`
- A `src/engine/schedule/__tests__/scheduleAdapter.getDailyWorkout.test.js` — 15 tests rest-day/training/hard-halt/blueprint-aggregate/sessionBuilder-delegate/equipment-filter

## Wording autonomous compose
- `workoutTitle` default `"Antrenament azi"` (ZERO diacritice)

## Acceptance criteria
- [x] `getDailyWorkout()` NEW export LANDED
- [x] Invoke `runPipeline(ctx, [8 adapters])` complete chain
- [x] sessionBuilder integration via `buildSession(sessionType, ctx)` real signature
- [x] Tests +15 PASS (target +12 minim)
- [x] TS 0 errors
- [x] ZERO `src/engine/*` mutation EXCEPȚIE acceptată `scheduleAdapter.js` per ORCHESTRATOR §7

## Deviation
- Orchestrator §B sketch called `buildSession({userState, periodization, ...})` cu obiect; actual signature `buildSession(sessionType, ctx)`. Adjusted per sketch §C "Verify actual signature; adjust call shape conform existing export." sessionType derives din `DAY_TO_SESSION_TYPE[dayIdx]` deterministic mapping (L=PUSH, M=PULL, M2=UPPER_PICIOARE, J=UMERI_BRATE, V=FULL_UPPER, S=PUSH, D=PULL).
