// Auth (route '/auth') — top-level, no bottom nav (web router.tsx L152).
import { Placeholder } from '../../components/Placeholder';
export default function Auth() {
  return <Placeholder name="Auth" route="/auth" />;
}
