// AuthCallback (route '/auth-callback') — Magic Link landing (web router.tsx L154).
import { Placeholder } from '../components/Placeholder';
export default function AuthCallback() {
  return <Placeholder name="Auth Callback" route="/auth-callback" />;
}
