// Terms (route '/terms') — public legal page, top-level (web router.tsx L157).
import { Placeholder } from '../components/Placeholder';
export default function Terms() {
  return <Placeholder name="Termeni" route="/terms" />;
}
