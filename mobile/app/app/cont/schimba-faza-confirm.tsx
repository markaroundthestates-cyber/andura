// schimba-faza-confirm (route '/app/cont/schimba-faza-confirm') — real body ported in W6b.
// Placeholder leaf so W6a navigation targets (Cont hub rows + SettingsPrefs
// advanced drill-downs) do not dead-end (mirror restore-account.tsx pattern).
import { Placeholder } from '../../../components/Placeholder';
export default function SchimbaFazaConfirm() {
  return <Placeholder name="Schimba faza" route="/app/cont/schimba-faza-confirm" />;
}
