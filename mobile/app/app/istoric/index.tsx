// Istoric tab root (route '/app/istoric') — web router.tsx L202.
import { Placeholder } from '../../../components/Placeholder';
export default function Istoric() {
  return <Placeholder name="Istoric" route="/app/istoric" />;
}
