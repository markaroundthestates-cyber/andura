{
  "name": "andura-functions",
  "description": "Andura Cloud Functions - FCM push notification scheduler",
  "private": true,
  "type": "commonjs",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node -c index.js && node -c scheduler.js && node -c scheduler.test.js && node -c deletionGrace.js && node -c deletionGrace.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.0.0"
  }
}
