// Intentionally empty scratch file (superseded by sim-machinery.test.js).
// The harness blocked deletion mid-task; left as a no-op skipped placeholder so
// the suite collects it cleanly (passWithNoTests:false). Safe to delete.
import { it } from 'vitest';
it.skip('placeholder — see sim-machinery.test.js', () => {});
