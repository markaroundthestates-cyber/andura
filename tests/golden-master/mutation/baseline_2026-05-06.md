# Stryker Baseline Mutation Testing Audit — 2026-05-06

**Run config:** `tests/golden-master/mutation/stryker.conf.js`
**Scope:** `src/**/*.js` (excluding `*.test.js` + `__tests__`)
**Status:** ⚠ **RUN IN PROGRESS** — initial test run + 23,079 mutant evaluation started 2026-05-06 01:10. CPU-bound long ~6-12h on i7-8700K 6c/12t per orchestrator estimate.

## Pre-run config + sanity

- Stryker installed: `@stryker-mutator/core@9.6.1` + `@stryker-mutator/vitest-runner` (npm install --save-dev complete)
- Config replaced: `tests/golden-master/mutation/stryker.conf.js` cu Bugatti baseline scope `src/**/*.js` (NU cherry-pick)
- Initial scan: **134 source files** found pentru mutation (post auth phase 2 batch 2+3 LANDED state — captures actual codebase ACTUAL state)
- Instrumentation: **23,079 mutants** generated
- Concurrency: 6 test runner processes (calibrat i7-8700K reserve 2 logical cores OS)
- Timeout: 10000ms per mutant
- Mutator excludes: `StringLiteral` (avoid trivial wording mutations noise)
- Thresholds first-run baseline: `break: 0` (capture state, NU CI fail)

## Run status

Background process started cu command:

```sh
npx stryker run tests/golden-master/mutation/stryker.conf.js 2>&1 | tee tests/golden-master/mutation/stryker-run.log
```

Initial log captured în `tests/golden-master/mutation/stryker-run.log`:

```
01:10:22 INFO ProjectReader Found 134 of 524 file(s) to be mutated.
01:10:25 INFO Instrumenter Instrumented 134 source file(s) with 23079 mutant(s)
01:10:25 INFO ConcurrencyTokenProvider Creating 6 test runner process(es).
01:10:27 INFO DryRunExecutor Starting initial test run (vitest test runner with "perTest" coverage analysis). This may take a while.
```

**Estimated completion:** ~6-12h (CPU-bound, 23,079 mutants × full test suite per-test coverage analysis).

## Daniel post-run action (when run completes)

When background run completes (Daniel poll dimineața via `tail tests/golden-master/mutation/stryker-run.log` sau check `reports/mutation/mutation.json` exists), generate aggregated audit raport per format below:

### Format aggregated raport (extend acest file post-run)

```markdown
## Overall Mutation Score (post-run)

- **Killed:** N (X.X%)
- **Survived:** N (X.X%)
- **No coverage:** N (X.X%)
- **Timeout:** N (X.X%)
- **Errors:** N (X.X%)

**Overall mutation score:** X.X%

## Per-cluster breakdown

| Cluster | LOC | Mutants | Killed | Survived | Score % | Status |
|---------|-----|---------|--------|----------|---------|--------|
| `src/engine/**` (35 engines) | ... | ... | ... | ... | ... | ✅ ≥80 / 🟡 60-79 / 🔴 <60 |
| `src/validation/**` | ... | ... | ... | ... | ... | ... |
| `src/simulator/**` | ... | ... | ... | ... | ... | ... |
| `src/storage/**` (db.js + Dexie) | ... | ... | ... | ... | ... | ... |
| `src/util/**` | ... | ... | ... | ... | ... | ... |
| `src/auth.js` + `src/firebase.js` | ... | ... | ... | ... | ... | ... |
| `src/pages/**` (incl. settings.js NEW) | ... | ... | ... | ... | ... | ... |
| `src/components/**` (incl. modals NEW) | ... | ... | ... | ... | ... | ... |
| `src/main.js` + entry points | ... | ... | ... | ... | ... | ... |

## Bugatti benchmark vs actual

- **Safety paths target:** ≥80% (4-Invariant Safety Stack §42.9 + 5th Medical Safety §50.3.10)
  - Actual: X.X% — ✅/🟡/🔴
- **Non-safety target:** ≥70%
  - Actual: X.X% — ✅/🟡/🔴

## Top 20 Survived Mutants Prioritized

### Priority 1 — CRITICAL Safety Paths (orice survived = bug silent waiting în production)

[Extract from reports/mutation/mutation.json — filter status=Survived, file include 'invariants' OR 'safety' OR 'medical']

### Priority 2 — Engine logic (`src/engine/**/*.js`)

### Priority 3 — Auth + Firebase (post batch 2+3 NEW code paths)

### Priority 4 — Validation + Simulator

### Priority 5 — Storage (db.js + Dexie + migration)

### Priority 6 — Util

## Action items pentru Daniel review

1. **Critical safety mutants survived:** list paths + recommended test additions
2. **Clusters sub Bugatti threshold:** list + recommended priority fixes
3. **Test gaps identified:** specific patterns missing
4. **Stryker timeouts/errors:** investigate root cause if non-trivial count

## Recommendations

- **Threshold settings post-baseline:** recommend `break: 75` for CI integration future once known-survived addressed
- **Schedule next mutation run:** post Engine #2 ADR 024 spec + adapter wiring stable
- **Excluded mutations review:** consider adding/removing per noise/signal ratio observed
```

## Stryker run completion check (Daniel manual)

```powershell
# Check if run still running:
Get-Process -Name node -ErrorAction SilentlyContinue | Select-Object Id, ProcessName, StartTime

# Or check log tail:
Get-Content tests/golden-master/mutation/stryker-run.log -Tail 50

# Check if reports written:
Test-Path reports/mutation/mutation.json
Test-Path reports/mutation/mutation.html
```

## Verify ZERO src/ modifications

`git diff --stat src/` post-run should show no diff (Stryker is read-only on src/, only writes la `reports/mutation/` + `tests/golden-master/mutation/`).
