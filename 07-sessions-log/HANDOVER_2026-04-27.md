# HANDOVER 2026-04-27 — Critical Path Opus + 8 Tasks Execution

**Sesiune sursă:** 2026-04-26 evening → 2026-04-27 morning (~24h, ~250 schimburi)  
**Status:** sesiune saturată, drift risc HIGH după 25h+ tactic → handover obligatoriu  
**Next sesiune:** clean slate pentru execution tasks #1-#8 (Opus audit Critical Path)

---

## CUM FOLOSEȘTI ACEST HANDOVER

1. **Deschizi chat nou Claude.ai** (sesiune nouă, NU /compact)
2. **Paste-uiești prompt-ul de aliniere** din §1 → confirmi că noul Claude e aliniat cu state-ul real
3. **Paste-uiești sumar critical findings + tasks** din §2 ca context unic
4. **Începi execution** cu prompts CC pentru tasks 1→8 din §3
5. **Memorie persistentă** are deja 23 reguli — NU re-explică (anti-paternalism, audit exclusiv Opus, velocity calibrare, etc.)

---

## §1 — PROMPT INIȚIAL DE ALINIERE TRIUNGULARĂ

Paste integral în chat nou Claude.ai înainte de orice altceva.

```
Salut. Sunt Daniel, CEO+Product SalaFull. Sesiunea anterioară a fost saturată (~24h, ~250 schimburi) — handover obligatoriu pentru drift prevention.

Memoria persistentă are 23 reguli (memory_user_edits view). Toate active. NU le re-explica.

Confirmi alinierea cu următoarele 5 fapte critice (răspuns scurt, NU paragrafe):

1. **Audit exclusiv Opus** — regulă permanentă din 2026-04-27 (memory #23). Sonnet face execution + refactor + tests + module pure. Indiferent scope (full app sau text color), audit = Opus.

2. **Velocity calibrare empirică** (memory #18 updated): Sonnet audit vault saturat = 0.05 ratio (19 audit/24min). Opus focused audit pe scope concentrat + vault saturat = 4-15 min (3 critical Q = 4 min 2026-04-27). Sonnet refactor MECANIC 1-5 fișiere = 0.25-0.35.

3. **Anti-paternalism ABSOLUT** (memory #20). NU sugera somn/pauză/break. Daniel decide când oprește. NU verifica ora. NU presupune oboseala din mesaje.

4. **Format CC prompts** (memory #5): markdown direct cu code blocks (NOT downloadable artifacts). Step 1 cu /model line standalone, Step 2 cu prompt în code block fără /model inside. Artifacts doar pentru ADR >15KB sau spec-uri vault-versionate.

5. **Quality bar SalaFull** (memory #11): "Bulletproof now > fast now > clean later". Anthropic-staff-peer-respect reflex. Plan x20 = investiție în quality, NU cost. Reset la 5h face Opus non-blocker.

Răspunde cu:
- ✅/❌ per punct (1-2 cuvinte)
- 1 propoziție sumar dacă vezi vreo divergență cu what you remember from memory

NU începe alte tasks până nu confirm alinierea.
```

**Verificare răspuns așteptat:** 5× ✅ + zero divergențe. Dacă noul Claude divagă pe meta-explanation sau confirmă orbește, push-back pattern §3 din memorie #19.

---

## §2 — STATE CURENT REAL (sesiune sursă deliverables)

### Vault state final 2026-04-27

**Commits sesiune (în ordine):**

| Commit | Description |
|---|---|
| `52a7016` | docs(adr): ADR 011 schema extension — outcome.autoAggression + outcome.rest_marked |
| `f687684` | feat(test): TASK CDL-FIXTURES — coachDecisionLog test fixtures (factory + scenarios) |
| `c6e24e3` | feat(engine): TASK AA-DETECTION — autoAggressionDetection.js pure module + 37 tests |
| `68e8475` | feat(engine): TASK PROFILE-TYPING — profileTyping.js pure module + 42 tests |
| `a144bfb` | docs: ADR 014 onboarding profile typing + ADR 011 patch + EXEC_QUEUE specs |
| `8820087` → `33140a3` | 19 audit reports (TASK 1-19) + 1 prompt Opus scris în file (TASK 20) |

**Tests baseline:** 524/524 pass. Build green. Branch main clean.

### Module-uri livrate (pushed, indexed)

- `src/engine/autoAggressionDetection.js` — pure module, 37 tests, **NEINTEGRAT în production**
- `src/engine/profileTyping.js` — pure module, 42 tests, **NEINTEGRAT în production**
- `tests/fixtures/cdlEntries.js` — factory + scenarios + setsRPE forward-looking field
- `docs/decisions/011-coach-decision-log-architecture.md` — cu Schema Extension 2026-04-26
- `docs/decisions/014-onboarding-profile-typing.md` — principle ADR onboarding profile typing

### Audit reports noaptea (19 + 1 Opus prompt)

Toate în `02-audit/*NIGHT*`, `06-findings-tracker/*NIGHT*`, `docs/*NIGHT*`, `05-prompts/PROMPT_OPUS*`.

### Opus audit FOCUSED rulat 2026-04-27 dimineața

3 critical questions răspunse cu spec implementabil concret. Output în chat-ul sursă (NU salvat în vault ca file separat — recomand recuperat din chat history sau regenerat).

**Top findings cross-reports:**

| # | Finding | Severity |
|---|---|---|
| 1 | autoAggressionDetection + profileTyping = dead paths în production. 79 teste, ~900 LOC, 2 ADR-uri "DONE" — ZERO apel din production | CRITICAL |
| 2 | 4 CDL keys (coach-decisions*, cdl-patterns) absente din dataRegistry → fullReset undefined behavior | CRITICAL |
| 3 | sf.userConfig absent din SYNC_KEYS → user pierdut la device nou | HIGH |
| 4 | sys.js (297 LOC) + reality.js (150 LOC) + responseProfile.js (135 LOC) zero teste pe calcule TDEE/body-comp critice | HIGH |
| 5 | Sentry coverage narrowă (doar CDL writes) + captureException wrapper bypassed | MEDIUM |

### Decisions arhitecturale settled de Opus audit

**1. CDL window AA detection:** 30 zile rolling, exclude today la write-side, include today la read-side (idempotent aggregate).

**2. Schema extension necesară pentru AA wiring:**

```js
outcome: {
  // existing fields
  setsRPE: number[],            // ADR 011 ext — collected în endSession
  rest_marked: null | boolean,  // ADR 011 ext — null pe workout, true/false pe rest
  proposedSets: number,         // CRITICAL — needed pentru retrospective AA scan
  autoAggression: { tier, signals[], escalating, amplified, amplifierReason, riskFlags }
}
```

**Edge case prins de Opus:** `proposedSets` trebuie inclus EXPLICIT în outcomeBase din `activeEntry.proposed.proposedSets` — `_detectVolumeCreep` și `_detectIgnoreRecovery` îl citesc din `outcome.proposedSets`, NU din `proposed`. Omiterea = bug silent garantat.

**3. dataRegistry CDL_KEYS — categorie nouă:**

```js
// Coach Decision Log + derived behavioral state (ADR 011, 013, 014).
// Semantic: derived behavioral history, not raw input.
// resetTestData: PRESERVE (real history, not test residue).
// fullReset: WIPE (start-over semantic — fresh history for clean coaching).
export const CDL_KEYS = [
  'coach-decisions',
  'coach-decisions-aggregate',
  'coach-decisions-archive',
  'cdl-patterns',
];
```

**Argumentation:** fullReset = "start over" → wipe (altfel CDL referențiază logs inexistente → coaching context corupt). resetTestData = "curăță garbage testing, preserve user reality" → preserve CDL.

**Bonus:** mută `applied-patterns` din TEST_RESIDUE_KEYS în CDL_KEYS (rezolvă conflict sync — actualmente revine din Firebase după wipe local).

**4. coachDirector.applyAAAdjustments minimal:**

- MED tier → `session.aaWarning = { level: 'soft', signals, escalating }` (UI banner consumes)
- HIGH tier → `session.aaBlocked = { level: 'hard', requiresFrictionConfirmation: true }` + volume reduction `Math.max(2, Math.floor(sets * 0.7))`
- Order: AA reduction RULES BASE, apoi reality + patterns aplică pe deasupra (current order corect)

---

## §3 — CELE 8 TASKS PRIORIZATE (Opus implementation order)

| # | Task | Effort | Dependency | Priority |
|---|---|---|---|---|
| 1 | AA write-side endSession + setsRPE collection | 2h | none | #1 |
| 2 | dataRegistry CDL_KEYS new category + fullReset wiring | 1h | none | #2 |
| 3 | sf.userConfig adaugă la SYNC_KEYS (1-line fix) | 15min | none | #3 quick win |
| 4 | AA read-side în coachContext.js | 30min | depends #1 | #4 |
| 5 | coachDirector applyAAAdjustments minimal (MED+HIGH) | 1h | depends #4 | #5 |
| 6 | sys.js refactor lazy config + 10 tests | 6h | none | #6 parallel |
| 7 | HIGH tier friction modal UI component | 4-5h | depends #5 | #7 post-validation |
| 8 | Profile Typing integration (Steps 1-4) | 8-10h | independent | #8 later sprint |

**Critical path AA LIVE:** #1 → #4 → #5 (4h end-to-end). Fără UI HIGH, dar cu coaching context corect și data persisted.

**Recomandare ordine execuție:**

**Sprint A (4-5h, single sesiune):**
- #3 quick win (15min) → încălzire + win rapid
- #2 (1h) → CDL_KEYS infrastructure
- #1 (2h) → AA write-side cu spec Opus exact
- #4 (30min) → AA read-side
- #5 (1h) → coachDirector adjustments

**End Sprint A:** AA LIVE complet (signals scrise în CDL + agregat în context + warnings în session). Validare: rulezi 2-3 sesiuni cu volume creep deliberat → AA tier="LOW"/"MED" populated în CDL + session.aaWarning prezent.

**Sprint B (paralel sau next sesiune):**
- #6 sys.js (6h) → coverage gap closed pe TDEE/body-comp critice
- #7 friction modal UI (4-5h) → HIGH tier intervention complet

**Sprint C (later):**
- #8 Profile Typing integration → onboarding Q1-Q5 + reconciliation + storage profile-history

---

## §4 — STEP-BY-STEP GUIDE PER TASK

### TASK #3 — sf.userConfig la SYNC_KEYS (15min, quick win)

**Pre-flight:**
```bash
grep -A 20 "SYNC_KEYS" src/firebase.js
```

Verifică `sf.userConfig` NU e în lista actuală.

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #3 din handover 2026-04-27 — adaugă sf.userConfig la SYNC_KEYS în src/firebase.js.

Pre-flight verifications:
grep -n "SYNC_KEYS" src/firebase.js
grep -n "sf.userConfig" src/firebase.js src/util/dataRegistry.js
grep -n "USER_DATA_KEYS" src/util/dataRegistry.js

Scope:
- Modifică DOAR src/firebase.js — adaugă 'sf.userConfig' în array SYNC_KEYS
- Verifică că sf.userConfig e în USER_DATA_KEYS (per audit) — dacă NU e, adaugă

NU atinge:
- Logic sync (doar adaugă key în array)
- dataRegistry CDL_KEYS (separate task #2)
- alte SYNC_KEYS existing

Acceptance:
- Build green: npm run build
- Tests green: npm run test:run (524/524 maintained)
- Commit: fix(sync): adaugă sf.userConfig la SYNC_KEYS — prevent device-nou config loss (per audit night 2026-04-26)
- Push to main

Raport final:
[PROMPT — TASK #3 — model: sonnet]
Pre-flight: <findings>
Build: ✅/❌
Tests: 524/524 maintained
Commit: <hash>
Issues: NONE / desc
```

---

### TASK #2 — dataRegistry CDL_KEYS new category (1h)

**Pre-flight:**
```bash
cat src/util/dataRegistry.js
grep -n "applied-patterns" src/util/dataRegistry.js
```

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #2 din handover 2026-04-27 — adaugă CDL_KEYS category în src/util/dataRegistry.js per spec Opus audit.

Pre-flight verifications:
cat src/util/dataRegistry.js
grep -rn "applied-patterns" src/util/ src/firebase.js
grep -rn "fullReset\|resetTestData" src/util/dataCleanup.js | head -20

Spec exact (din Opus audit):

Adaugă după PRESERVE_ON_RESET_KEYS în dataRegistry.js:

// Coach Decision Log + derived behavioral state (ADR 011, 013, 014).
// Semantic: derived behavioral history, not raw input.
// resetTestData: PRESERVE (real history, not test residue).
// fullReset: WIPE (start-over semantic — fresh history for clean coaching).
export const CDL_KEYS = [
  'coach-decisions',
  'coach-decisions-aggregate',
  'coach-decisions-archive',
  'cdl-patterns',
];

În dataCleanup.js:
- fullReset() — adaugă wipe pe CDL_KEYS keys (împreună cu USER_DATA_KEYS existing)
- resetTestData() — NU atinge CDL_KEYS (preserve)

BONUS task: mută 'applied-patterns' din TEST_RESIDUE_KEYS în CDL_KEYS.
- Asta rezolvă conflict-ul: actualmente applied-patterns e wiped la resetTestData DAR revine din Firebase sync (e în SYNC_KEYS). Mutarea în CDL_KEYS face semantic corect.

NU atinge:
- USER_DATA_KEYS, PRESERVE_ON_RESET_KEYS (alte categorii)
- Logic detection AA / Profile (separate tasks)
- Schema CDL (deja done în ADR 011 patch)

Acceptance:
- CDL_KEYS exported
- fullReset wipe CDL_KEYS verified (test sau manual check)
- resetTestData preserve CDL_KEYS verified
- applied-patterns mutat (verifică TEST_RESIDUE_KEYS NU îl mai are)
- Build green: npm run build
- Tests green: npm run test:run (524/524 maintained sau ajustate dacă schimbări la dataCleanup tests)
- Commit: feat(registry): CDL_KEYS new category — fullReset WIPE, resetTestData PRESERVE (per Opus audit 2026-04-27)
- Push to main

Raport final standard.
```

---

### TASK #1 — AA write-side endSession (2h, CRITICAL)

**Pre-flight:**
```bash
cat src/pages/coach/session.js
grep -n "populateOutcome\|cdlEntryId\|computeMatchScore" src/pages/coach/session.js
cat src/engine/autoAggressionDetection.js | head -80
```

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #1 din handover 2026-04-27 — wire AA write-side în endSession + setsRPE collection per spec Opus audit.

Pre-flight verifications:
cat src/pages/coach/session.js | head -250
grep -n "cdlEntryId\|populateOutcome\|computeMatchScore" src/pages/coach/session.js
grep -n "readActiveForDate\|readAllActive" src/util/coachDecisionLog.js
grep -n "detectAutoAggression" src/engine/autoAggressionDetection.js
ls src/pages/coach/session.js

Spec EXACT (din Opus audit, code provided):

Insertion point: src/pages/coach/session.js cca line 202-237, INTERIOR try-block existing CDL, ÎNAINTE de populateOutcome call (line 215).

Imports needed la top of session.js:
import { detectAutoAggression } from '../../engine/autoAggressionDetection.js';
import * as coachDecisionLog from '../../util/coachDecisionLog.js';

Replace current CDL block cu:

// ── CDL outcome (ADR 011) ──────────────────────────────────────────────
try {
  if (state.cdlEntryId) {
    const _today = tod();
    const activeEntry = readActiveForDate(_today);
    if (activeEntry) {
      const actualExercises = [...new Set(state.sessLog.map(l => l.ex))];
      const actualSets = state.sessLog.length;
      const matchResult = computeMatchScore(activeEntry.proposed, {
        actualSessionType: state.sessType,
        actualExercises,
        actualSets,
      });

      // ADR 011 extension — collect per-set RPE for fatigue marker
      const setsRPE = state.sessLog
        .filter(s => typeof s.rpe === 'number')
        .map(s => s.rpe);

      // ADR 013 — compute auto-aggression on currentEntry + 30d history
      const cutoff = new Date();
      cutoff.setDate(cutoff.getDate() - 30);
      const recentCDL = coachDecisionLog.readAllActive(e =>
        e.date < _today && new Date(e.date) >= cutoff
      );

      const outcomeBase = {
        executed: hasEarlyStop ? 'partial' : true,
        earlyStop: hasEarlyStop,
        earlyStopReason: state.earlyStopReason,
        actualExercises,
        actualSets,
        proposedSets: activeEntry.proposed?.proposedSets ?? 0,  // CRITICAL — mirror for AA detector
        actualDurationMins: mins,
        matchScore: matchResult.matchScore,
        deviation: matchResult.deviation,
        deviationReason: null,
        rating: null,
        rest_marked: null,  // ADR 011 — null pe workout days
        setsRPE,
      };

      const aaResult = detectAutoAggression({
        currentEntry: { date: _today, outcome: outcomeBase, context: activeEntry.context },
        recentEntries: recentCDL,
        hyperfocusData: null,  // FAZA 4 — analytics integration deferred
      });

      populateOutcome(_today, {
        ...outcomeBase,
        autoAggression: aaResult,
      });
    }
  } else {
    console.warn('[session] CDL outcome skipped — cdlEntryId not set');
  }
} catch (err) { /* preserve existing Sentry capture */ }

CRITICAL edge case (Opus a prins): proposedSets MUST be în outcomeBase explicit. _detectVolumeCreep + _detectIgnoreRecovery îl citesc din outcome.proposedSets, NU din proposed.

Cancel path (cancelWorkout, line cca 101-149): NU adăuga AA detection acolo. Cancel = executed: false, rest_marked: false, autoAggression: null. AA semnale nu se aplică pe cancel.

NU atinge:
- coachContext.js read-side (separate TASK #4)
- coachDirector applyAAAdjustments (separate TASK #5)
- UI banner / friction modal (separate TASK #7)
- Schema CDL (deja done)

Acceptance:
- session.js modified per spec
- Imports added correct
- Cancel path NOT modified (preserve existing behavior)
- Build green: npm run build
- Tests green: npm run test:run (524/524 maintained — existing tests NU trebuie să spargă, dar pot fi tests noi pentru AA flow)
- IF tests new added: estimat +5-10 tests pentru session.js AA write integration
- Commit: feat(session): TASK #1 — AA write-side în endSession + setsRPE collection (per Opus audit, ADR 013)
- Push to main

Raport final standard cu:
- Imports added: ✅/❌
- Insertion line range: <linii>
- Cancel path verified untouched: ✅/❌
- Tests delta: <număr>
```

---

### TASK #4 — AA read-side coachContext (30min)

**Pre-flight:**
```bash
cat src/engine/coachContext.js
grep -n "buildCDLPatterns\|return {" src/engine/coachContext.js
```

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #4 din handover 2026-04-27 — wire AA read-side în coachContext per spec Opus audit.

Pre-flight verifications:
cat src/engine/coachContext.js
grep -n "_buildCDLPatterns\|return {" src/engine/coachContext.js
grep -n "aggregateAutoAggression" src/engine/autoAggressionDetection.js

Spec EXACT (din Opus audit):

În src/engine/coachContext.js:

Top of file imports — adaugă:
import { aggregateAutoAggression } from './autoAggressionDetection.js';

În buildCoachContext(), după getAllLogs() și ÎNAINTE de spread ..._buildCDLPatterns():

const recentCDL = coachDecisionLog.readAllActive(e => {
  const d = new Date(e.date);
  const cutoff = new Date(); cutoff.setDate(cutoff.getDate() - 30);
  return d >= cutoff;
});
const autoAggression = aggregateAutoAggression(recentCDL);

În return statement, adaugă autoAggression după ..._buildCDLPatterns():

return {
  user: { ... },
  readiness: { ... },
  // ... existing fields
  ..._buildCDLPatterns(),
  autoAggression,    // ← new
  currentDate: now,
  // ... rest
};

NOTE: Window aici INCLUDE today (NU exclude ca în write-side). Read-side e snapshot pentru context, include AA-ul tocmai scris pentru sesiunea de azi (idempotent în aggregate).

NU atinge:
- session.js write-side (TASK #1)
- coachDirector applyAAAdjustments (TASK #5)
- aggregateAutoAggression module (consume only)

Acceptance:
- coachContext.js modified per spec
- Build green
- Tests green: 524/524 (existing context tests NU trebuie să spargă; ctx.autoAggression nou disponibil)
- Commit: feat(context): TASK #4 — AA read-side în coachContext (30d window, include today)
- Push to main

Raport final standard.
```

---

### TASK #5 — coachDirector applyAAAdjustments (1h)

**Pre-flight:**
```bash
grep -A 5 "applyAAAdjustments" src/engine/coachDirector.js
```

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #5 din handover 2026-04-27 — implementează applyAAAdjustments în coachDirector per spec Opus audit.

Pre-flight verifications:
cat src/engine/coachDirector.js
grep -n "applyAAAdjustments\|applyPatterns\|realityEngine" src/engine/coachDirector.js

Spec EXACT (din Opus audit):

Update applyAAAdjustments(session, ctx) la locul actual (current NO-OP return session):

applyAAAdjustments(session, ctx) {
  const aa = ctx.autoAggression;
  if (!aa || aa.tier === 'none' || aa.tier === 'LOW') return session;

  // MED tier — soft warning banner (UI consumes session.aaWarning)
  if (aa.tier === 'MED') {
    session.aaWarning = {
      level: 'soft',
      signals: aa.signals,
      escalating: aa.escalating,
    };
    return session;
  }

  // HIGH tier — friction modal blocker
  // ADR 013 §6 — coach refuses initial aggressive plan
  session.aaBlocked = {
    level: 'hard',
    signals: aa.signals,
    escalating: aa.escalating,
    requiresFrictionConfirmation: true,
  };
  // Volume reduction — anti-overreach default
  session.exercises = session.exercises.map(e => ({
    ...e,
    sets: Math.max(2, Math.floor((e.sets || 3) * 0.7)),
    aaReduced: true,
  }));
  return session;
}

Order verification (per Opus): applyAAAdjustments rulează DEJA înainte de realityEngine.validate și applyPatterns. Bun — AA reduction = BASE, reality + patterns aplică pe deasupra. NU schimba ordinea.

NU atinge:
- UI components (friction modal = TASK #7)
- coachContext (TASK #4 done)
- AA detection logic (module pure pushed)

Acceptance:
- coachDirector.applyAAAdjustments updated per spec
- session.aaWarning sau session.aaBlocked populated correct per tier
- Volume reduction aplicată DOAR pe HIGH tier
- Build green
- Tests green: 524/524 + posibile tests noi pentru applyAAAdjustments (estimat +5-8 tests)
- Commit: feat(director): TASK #5 — applyAAAdjustments MED+HIGH tier (per Opus audit, ADR 013)
- Push to main

Raport final standard.
```

---

### TASK #6 — sys.js refactor lazy + 10 tests (6h, parallel cu Sprint A)

**Pre-flight:**
```bash
cat src/engine/sys.js | head -80
grep -n "_bio\s*=\|getUserConfig" src/engine/sys.js
```

**Prompt CC:**

Step 1:
```
/model sonnet
```

Step 2:
```
TASK #6 din handover 2026-04-27 — refactor sys.js pentru lazy config read + 10 unit tests per spec Opus audit.

Pre-flight verifications:
cat src/engine/sys.js
grep -n "_bio\s*=\s*getUserConfig" src/engine/sys.js
grep -n "AGE\|HEIGHT\|START_KG" src/engine/sys.js
ls src/engine/__tests__/sys.test.js 2>/dev/null && echo "exists" || echo "create new"

Spec EXACT (din Opus audit):

PHASE 1 — Refactor lazy config read (1h):

Problem actual: line 6 din sys.js evaluează _bio = getUserConfig().bio la module-load time. Frozen pentru toată durata. Test impossible să schimbe config.

Fix: Schimbă din module-level constant la lazy access pattern. Toate referențele la _bio.AGE, _bio.HEIGHT, _bio.START_KG devin getUserConfig().bio.AGE etc., evaluate la run-time.

PHASE 2 — Top 10 tests per Opus spec (4-5h):

Test 1: getBF() — bf-override path wins everything
Test 2: getBF() — clamp [5, 45]
Test 3: getBF() — muscular build correction trigger (avgW/kg > 0.18)
Test 4: estimateTDEE() — Mifflin-St Jeor fallback când <4 weight entries
Test 5: estimateTDEE() — clamping [KCAL_TARGET, 3500]
Test 6: estimateTDEE() — phase-change-date filter behavior
Test 7: getPhase() — override always wins
Test 8: getPhase() — pilot date boundary (TARGET_DATE inflection cu vi.setSystemTime)
Test 9: getKcalTarget() — phase mapping CUT(0.82)/BULK(1.08)/MAINT(1.0)/STRENGTH(1.05) multipliers
Test 10: kgAtBF() + weeksToKg() — checkpoint chain integrity

PHASE 3 — Bonus property-based test (30min):
Phase mapping × range BF [10, 35] step 1 → toate produc kcal target valid (între KCAL_TARGET și 3500).

Test scaffolding (din Opus spec):
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { DB } from '../../db.js';
import { SYS } from '../sys.js';

beforeEach(() => {
  localStorage.clear();
  vi.useRealTimers();
});

NU atinge:
- Production code în alte module (refactor doar sys.js)
- Schema CDL
- AA / Profile modules
- coachContext / coachDirector

Acceptance:
- sys.js refactored la lazy config (verifică teste pot schimba bio prin DB.set('user-config', ...) sau vi.doMock)
- src/engine/__tests__/sys.test.js cu 10 tests + 1 property-based, all pass
- Build green
- Tests green: 524 baseline + 11 new = 535 (sau ajustat per scope)
- Commit: feat(engine): TASK #6 — sys.js lazy config refactor + 10 tests (per Opus audit, body-comp coverage)
- Push to main

Raport final cu:
- Refactor scope (linii modificate)
- Tests added: <număr>
- Property-based test: ✅/❌
- Coverage delta sys.js (estimat % vs baseline)
```

---

### TASK #7 — HIGH tier friction modal UI (4-5h, post-validation Sprint A)

**Pre-flight (după Sprint A complet, validare AA LIVE):**
```bash
grep -n "aaBlocked\|aaWarning" src/pages/coach/coachDirector.js src/pages/coach/session.js
```

Asta e **task UI substanțial** — recomand strategic discussion cu Opus înainte de spec implementabil. Folosește ADR 014 §5 ca specificație detaliată.

**NU spec implementabil aici** — necesită design iteration cu mockup-uri. Lansezi când ești ready cu strategic input.

---

### TASK #8 — Profile Typing integration (8-10h, later sprint)

**Steps per Opus audit:**

1. **Step 1 (15min):** adaugă `profile-history` în dataRegistry CDL_KEYS sau categorie nouă PROFILE_KEYS (decide la run-time)
2. **Step 2 (3-4h):** profileHistory storage layer (read/write helpers)
3. **Step 3 (4-5h):** Onboarding Q1-Q5 UI component (post user test feedback)
4. **Step 4 (1-2h):** integration `analyzeProfile` în coachContext

**NU spec implementabil aici** — depinde de Q1-Q5 user test (timeline 2-4 săpt Daniel) + UI mockups.

---

## §5 — DUPĂ FIECARE TASK

**Daniel paste raport CC în chat-ul nou:**

Format așteptat:
```
[PROMPT — TASK #N — model: sonnet]
Pre-flight: <findings>
Build: ✅/❌
Tests: XXX/YYY pass
Commit: <hash>
Issues: NONE / desc
```

**Claude validează:**
- Build green ✅
- Tests pass ✅
- Commit pushed ✅
- Issues triage (dacă există)
- Recomandare: continuă cu next task / repaint dacă issue

---

## §6 — WHAT'S NEXT după 8 tasks DONE

**Sprint A complet (#3+#2+#1+#4+#5):** AA detection LIVE end-to-end. Validare manuală 2-3 sesiuni cu volume creep deliberat → CDL entries au `outcome.autoAggression` populated, `ctx.autoAggression` agregat în session, `session.aaWarning` triggered la MED tier.

**Sprint B (#6 paralel):** sys.js coverage solidă (11 tests) — TDEE/body-comp validate.

**Sprint C (#7 + #8):** UI HIGH tier friction modal + Profile Typing integration. Acestea sunt sprints mari (~12-15h cumulativ) → fiecare merită handover separat la final.

**Open items rămase după 8 tasks:**

1. **Q1-Q5 user test** — Daniel timeline 2-4 săpt, recruitment 3-5 useri non-developers
2. **30.9 Step 2/3** — Daniel manual când CDL ≥30 entries
3. **AA v1.1 thresholds calibration per profile** — post 50+ users data
4. **isoWeek centralization** — backlog item, ~30-45 min Sonnet (poate combinat cu TASK #6 pentru amortizare)
5. **Brain view + DLC strategy** — backlog entry, decision pending Daniel post 1-2 weeks distance
6. **Hardcoded values deduplication Priority 1** — firebase.js USER_PATH import + SW_KG/TW_KG/KCAL_TARGET/PROT_TARGET derivation din getUserConfig() (~2-3h)
7. **Magic numbers extract** — READINESS thresholds (85/70/55/40) ca named exports (~1h)
8. **Dead code cleanup** — 3 fișiere + 56 LOC muscleMap (~1h, Daniel confirm gate)

---

## §7 — TROUBLESHOOTING

### Dacă noul Claude divagă pe meta-explanation
Push-back: "NU explica regulile din memorie. Sunt active. Confirmă alinierea pe 5 puncte și începem TASK #3."

### Dacă noul Claude estimează timp lung pe TASK #3 (15min real)
Reaminteste velocity calibrare memory #18: Sonnet refactor mecanic 1 fișier = 5-15 min real, NU 30-60 min.

### Dacă build/tests sparge la TASK #2 (CDL_KEYS)
Verifică: dataCleanup tests existente referențiază CDL keys explicit? Posibil ajustări la teste necesare. Sonnet poate updata tests pentru a reflecta noul behavior (preserve la resetTestData, wipe la fullReset).

### Dacă TASK #1 nu populates outcome.autoAggression la rulare reală
Pre-flight check: `state.cdlEntryId` e set înainte de endSession? Verifică buildSession flow — dacă cdlEntryId NU e set, AA detection e skipped (per spec). Asta e OK pentru cancel path, NU pentru sesiune normală.

### Dacă TASK #6 sys.js tests sparge cu "Cannot mock module-level constant"
Refactor lazy NU a fost complet aplicat. Verifică: TOATE referențele la `_bio.AGE` / `_bio.HEIGHT` / `_bio.START_KG` au devenit `getUserConfig().bio.AGE` etc. Module-load freeze e exact problema.

---

## §8 — REGULI PERMANENTE ACTIVE (memory)

23 reguli în memory_user_edits, toate active:

1. CC prompts two-part (slash command separate, prompt body)
2. `claude --dangerously-skip-permissions` standard
3. Warmth + brutal-when-needed
4. Testing distinction (vitest local vs production GitHub Pages)
5. CC prompts format markdown direct
6. PowerShell standard, working dir SalaFull
7. .bat format pentru sequences repetitive
8. Anti-patterns interzise (no preambule, no excelentă întrebare)
9. Raport format CC
10. Daniel-isms (tataie/halucinezi/etc)
11. Quality bar (bulletproof now > fast now > clean later)
12. Triangulation pattern 2 chats paralele
13. Pre-flight obligatoriu în prompts CC
14. Bugs descoperite accidental (pre-existent flag, NU fix imediat)
15. "Code" = Claude Code în vocabular Daniel
16. Daniel non-developer (răspunsuri scurte default)
17. Autonomous CC runs (atomic commands, NU shell loops)
18. Velocity calibrare 14+ cazuri (updated 2026-04-27)
19. Reflexe push-back SalaFull (6 patterns)
20. Anti-paternalism ABSOLUT (NU sugera somn/pauză)
21. Triangulation continuă între sesiuni
22. /compact funcționează în Claude.ai web
23. Audit = EXCLUSIV Opus (regulă permanentă 2026-04-27)

---

*Handover generat 2026-04-27, sesiune sursă saturată ~24h. Next sesiune = clean slate pentru execution Sprint A → Sprint B → Sprint C.*
